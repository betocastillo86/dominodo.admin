export const environment = {
  production: true,
  // PLACEHOLDER — stage API URL (Azure App Service). Update after the API is deployed to stage.
  apiBaseUrl: 'https://dominodo-api-stage.azurewebsites.net/api/v1',
};
